= simple_form_for(nutrient, html: { class: '' }) do |form|
  = form.input :name
  = form.input :alias
  = form.input :unit
  = form.input :description
  = form.input :source
  = form.input :is_limiting
  = form.button :submit, class: "btn btn-success"
