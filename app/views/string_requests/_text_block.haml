- if text && text.strip != ""
  %div.shadow.p-3.mb-5.bg-white.rounded
    %p.text-monospace
      = text
- else
  %div.shadow.p-3.mb-5.bg-white.rounded
    %p.text-monospace
      = I18n.t('string_requests.empty_string')
